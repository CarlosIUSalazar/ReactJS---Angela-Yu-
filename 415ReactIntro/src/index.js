import React from "react";
import ReactDOM from "react-dom";

//ReactDOM.render(What to show , where to show it , tell me when that render function has completed )
ReactDOM.render(
  <div>
    <h1>Hello World!</h1>
    <p>This is a paragraph.</p>
  </div>,
  document.getElementById("root")
);
